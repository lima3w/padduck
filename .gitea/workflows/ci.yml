name: CI Test Suite

on:
  push:
    branches-ignore:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  ci-test:
    runs-on: linux

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21'
          cache: true

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Verify migration annotations
        shell: bash
        run: |
          bad_up=$(grep -rL '^\-\- +migrate Up' backend/migrations/*.up.sql 2>/dev/null)
          bad_down=$(grep -rL '^\-\- +migrate Down' backend/migrations/*.down.sql 2>/dev/null)

          if [ -n "$bad_up" ] || [ -n "$bad_down" ]; then
            echo "ERROR: missing sql-migrate annotation in:"
            echo "$bad_up $bad_down" | tr ' ' '\n' | grep -v '^$'
            exit 1
          fi

      - name: Run go vet
        working-directory: backend
        run: go vet ./...

      - name: Run backend tests
        working-directory: backend
        run: go test -race -count=1 ./...

      - name: Verify backend builds
        working-directory: backend
        run: go build -o /dev/null ./...

      - name: Verify frontend builds
        working-directory: frontend
        run: npm run build --silent